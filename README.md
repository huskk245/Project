To run the project
execute npm i
and npm run dev for both frontend and backen# Project Setup

## Installation and Running

1. Execute the following command to install dependencies:
   ```sh
   npm i
   ```

2. To start the frontend and backend, run:
   ```sh
   npm run dev
   ```

